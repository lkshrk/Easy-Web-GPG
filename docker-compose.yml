version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
    ports:
      - "8080:8080"
    environment:
      - MASTER_PASSWORD=${MASTER_PASSWORD:-changeme}
    volumes:
      - ./data:/data
    restart: unless-stopped
